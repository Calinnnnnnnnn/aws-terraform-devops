name: Terraform Pipeline

on:
  push:
    paths:
      - "**/*.tf"
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout step
        uses: actions/checkout@v4

      - name: Test step
        run: echo "Pipeline works"
